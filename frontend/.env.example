# Server-side URL used by the same-origin read-only API bridge.
BLUEPRINT_API_URL=http://127.0.0.1:8000

# Browser-facing base. Keep this same-origin unless a compatible API deployment exists.
NEXT_PUBLIC_BLUEPRINT_API_BASE_URL=/api/blueprint
