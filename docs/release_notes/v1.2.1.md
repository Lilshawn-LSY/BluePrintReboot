# v1.2.1-full-stack-validation-gate

## Summary

This release closes the validation gap between the existing Streamlit application, read-only FastAPI layer, and v1.2.0 TypeScript frontend shell. It adds no product feature and changes no user-data, API-response, storage, note, snapshot, or restore contract.

## Changes

- Shared PowerShell Node resolver: explicit `-NodeHome`, then `BLUEPRINT_NODE_HOME`, then `PATH`; both `node.exe` and `npm.cmd` are required and Node below 22.13.0 is rejected.
- Deterministic `frontend_setup.ps1` using only `npm ci`, plus opt-in frontend setup from `dev_setup.ps1`.
- Default full `dev_check.ps1` gate for smoke, full pytest, frontend lint, and one frontend build/test pass. `-PythonOnly` and `-SmokeOnly` are explicitly partial and non-release-qualified.
- Optional ignored `artifacts/validation-summary.json` containing normalized phase states and runtime/Git metadata without command output, secrets, environment values, or private library paths.
- Focused same-origin bridge tests for exact GET allowlisting, query forwarding, upstream 404 preservation, generic upstream/network 503 behavior, and absence of write methods.
- Independent GitHub Actions jobs for Python 3.12 and the minimum supported Node 22.13.1 line.

## Exact automated validation commands and results

| Command | Result |
|---|---|
| `.\.venv\Scripts\python.exe scripts\smoke_check.py` | Passed: 84 passed, 0 warnings, 0 failed. |
| `.\.venv\Scripts\python.exe -m pytest -q` | Passed: 476 passed, 0 failed, 0 skipped in 34.34s. |
| `. .\scripts\resolve_node.ps1; $node = Resolve-BlueprintNode -NodeHome 'C:\Users\Public\tool\node-v24.18.0-win-x64'; Push-Location frontend; Invoke-BlueprintNpm -Node $node -ArgumentList @('run','lint'); Pop-Location` | Passed with exit 0 and no lint findings. |
| `. .\scripts\resolve_node.ps1; $node = Resolve-BlueprintNode -NodeHome 'C:\Users\Public\tool\node-v24.18.0-win-x64'; Push-Location frontend; Invoke-BlueprintNpm -Node $node -ArgumentList @('test'); Pop-Location` | Build passed; 10 tests passed, 0 failed, 0 skipped. |
| `.\scripts\dev_check.ps1 -NodeHome 'C:\Users\Public\tool\node-v24.18.0-win-x64' -WriteEvidence` | Full validation passed: smoke 84/0/0, pytest 476 passed, lint passed, build passed, frontend tests 10/0/0. Evidence recorded all four phases as passed with full scope. |
| `.\scripts\dev_check.ps1 -PythonOnly` | Passed and printed `PARTIAL VALIDATION`; explicitly stated that it was not release-qualified. |
| `git diff --check` | Passed with no whitespace errors. |
| `git status --short` | Audited: only bounded source/docs/workflow changes are visible; ignored `node_modules`, build output, caches, and evidence are not staged. |

Frontend setup was also executed as `.\scripts\frontend_setup.ps1 -NodeHome 'C:\Users\Public\tool\node-v24.18.0-win-x64'`: `npm ci` added 492 packages, exited 0, and left the lockfile SHA-256 unchanged. The installed dependency tree reported 11 audit findings (2 low, 3 moderate, 6 high); dependency upgrades were outside this bounded release-gate change.

## Manual checks

- [ ] Start `.\scripts\run_api.ps1`.
- [ ] Start `.\scripts\run_frontend.ps1 -NodeHome <portable-node-directory>`.
- [ ] Verify Dashboard, Library, Papers, and one Paper Detail page against the local API.
- [ ] Stop FastAPI and verify the shell stays navigable with an explicit unavailable state.
- [ ] Confirm Streamlit launches separately with `.\scripts\run_app.ps1`.

Manual checks remain unchecked until actually performed.

## Compatibility and next milestone

The four API route shapes remain GET-only: `/health`, `/library/status`, `/papers`, and `/papers/{paper_id}`. Streamlit remains the write, note, PDF, and maintenance interface. The next product milestone is a separately designed read-only Reader/PDF vertical slice; PDF.js, PDF-serving routes, notes, and all write methods remain out of scope here.

The legacy project-level `backup_guideline.txt` is superseded by `docs/checklists/new_pc_restore_checklist.md`.

## Manual validation

Environment: managed military Windows workstation

- FastAPI launch at `http://127.0.0.1:8000`: PASS
- Direct health and papers API requests: PASS
- Frontend development server launch: PASS
- Frontend browser access through `http://localhost:3000`: PASS
- Dashboard and Papers route rendering: PASS
- Frontend-to-FastAPI bridge health and papers requests: PASS
- Query parameter forwarding: PASS
- FastAPI-offline bridge response: PASS — HTTP 503
- FastAPI-offline frontend unavailable-state rendering: PASS
- Sidebar navigation while API is offline: PASS
- Streamlit separate launch and basic regression: PASS
- GitHub Actions: NOT VERIFIED / PASS

Runtime note:

- Vinext listened on IPv6 loopback `::1:3000`.
- `http://localhost:3000` was reachable.
- `http://127.0.0.1:3000` was not reachable.
- This was an address-family binding difference, not a firewall failure.

This does not invalidate the completed automated build and test results, but live runtime validation remains open.
