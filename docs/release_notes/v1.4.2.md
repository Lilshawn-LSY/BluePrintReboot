# v1.4.2-post-merge-evidence-and-reader-runtime-closure

## Summary

v1.4.2 replaces the self-invalidating schema 3.0 release-state model with schema 4.0. The canonical manifest now separates the immutable v1.4.0 product baseline, completed PR #5 control-plane evidence, and a nullable repository-HEAD observation that is explicitly not a committed invariant.

The generated [Current Release Status](../CURRENT_RELEASE_STATUS.md) remains the single human-readable authority for current automated, manual, publication, and operational evidence. This release note does not duplicate mutable counts or current repository-head claims.

## Evidence boundaries

- The verified v1.4.0 tag and its target commit establish the immutable product baseline.
- PR #5 merge evidence and its successful PR-head workflow are recorded independently from the product baseline.
- No post-merge `main` workflow is claimed without direct evidence.
- Tag existence remains distinct from GitHub Release publication.
- The latest local validation is distinct from the two conflicting historical v1.4.0 smoke records, which remain preserved.
- PR #6 supplied API offline/restart recovery, large-PDF behavior, detailed browser Range inspection, and separate non-mutating Streamlit evidence. The merged schema-4 validator could not represent those completions consistently; v1.4.3 repairs that current-state model. Genuine clean-PC restore remains separately unverified.

## Scope

This change affects only release-state evidence, deterministic documentation generation, tracker export validation, and focused regression tests. It does not change product behavior, dependencies, API routes, Reader/PDF behavior, storage formats, or user data.

This note is historical context, not the current source of truth. Use the generated [Current Release Status](../CURRENT_RELEASE_STATUS.md).
