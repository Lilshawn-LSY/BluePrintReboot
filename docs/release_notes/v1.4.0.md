# v1.4.0-pdfjs-reader-foundation

## Summary

v1.4.0 replaces the web Reader's native browser viewer as the primary path with a controlled PDF.js canvas foundation. It keeps the existing read-only, stable-`paper_id`, managed-root PDF endpoint and same-origin bridge unchanged. Page navigation, bounded zoom, explicit loading/error/retry states, cleanup, safe diagnostics, and a conditional native fallback are included. Annotation, text/search layers, writes, storage migration, and broader UI work are not included.

## Reader architecture

- `PdfJsReader` is a client component and owns the canvas, semantic toolbar, loading/error states, retry, and fallback selection.
- `PdfReaderController` owns document-load cycles, page and zoom bounds, render cancellation, safe error classification, cleanup, and bounded diagnostics. It is framework-independent and tested with module-boundary PDF.js mocks.
- `pdfjs-adapter.ts` checks the browser boundary before dynamically importing `pdfjs-dist`. Server rendering does not import browser-only PDF.js code.
- The authoritative load uses the existing `/api/blueprint/papers/{paper_id}/pdf` same-origin route. The former separate one-byte Range probe is removed.

## Package and worker strategy

`pdfjs-dist` is pinned at `5.7.284` in `package.json` and `package-lock.json`. The original `6.1.200` pin contains the pre-fix worker failure path documented by Mozilla PDF.js issue #11595 and fixed by merged upstream PR #20503: cancelling `loadingTask.destroy()` before loading completes can throw a second `Worker was terminated` error from the worker failure callback that application code cannot catch. The official 5.7.284 distribution contains that upstream fix; its worker handles an already-terminated failure callback by returning. This narrowly scoped package change was made only after verifying both installed worker sources.

The client adapter still dynamically imports `pdfjs-dist/build/pdf.worker.min.mjs?url` and assigns the emitted URL to `GlobalWorkerOptions.workerSrc`. Vinext/Vite therefore emits a hashed repository-built worker asset during the production build. There is no CDN, copied minified vendor bundle, arbitrary worker URL, fake-worker mode, disabled worker, or second PDF rendering library; once dependencies are installed, the build and runtime are local-first.

## Range delivery and security compatibility

No backend route or response shape changed. PDF.js reuses the existing GET-only endpoint and bridge. Stable `paper_id` resolution, managed-root containment, missing-paper/file behavior, full HTTP 200 delivery, HTTP 206 byte ranges, `Accept-Ranges`, `Content-Range`, exact lengths, malformed Range 400, and unsatisfiable Range 416 remain covered by disposable tests. The frontend accepts no path or arbitrary PDF URL and exposes no local filesystem path.

## Reader behavior

- The first page renders into one canvas after the PDF.js loading task resolves.
- Previous, next, and direct page entry clamp to the document bounds; first/last disabled states are visible.
- Zoom out, zoom in, and reset use a 50%-300% range in 25% steps and rerender only the active page.
- Loading and page-render progress are visible; safe unavailable, missing, load, and render failures offer retry.
- The native `<object>` viewer is mounted only after explicit fallback selection following a PDF.js failure. It is labeled, uses the same managed URL, and can return to PDF.js through retry.
- Buttons and input are keyboard-operable and labeled. The canvas has a page-specific accessible fallback description.

## Lifecycle and performance observability

One loading task is created per authoritative load cycle. Page navigation and zoom reuse the loaded document. New renders cancel or supersede active stale renders. Retry, fallback, `paper_id`/URL change, route exit, and component unmount clean the active render/page, loaded document, loading task, and canvas state. Cancellation promises are handled locally; errors are not globally suppressed.

The initial implementation had two overlapping lifecycle defects in the React development setup/cleanup/setup sequence:

1. Effect A could create a PDF.js task and then be cleaned up while `loadingTask.promise` was pending. `loadingTask.destroy()` correctly belonged to A, but PDF.js 6.1.200's worker could emit the upstream uncatchable termination error during that intentional cancellation.
2. A's asynchronous cleanup was started with `void`, and its controller retained access to the component's shared canvas and callbacks. Effect B created a separate controller and worker, but late A cleanup could still clear B's canvas or publish stale diagnostics after B became authoritative. Destruction also did not wait for a task whose asynchronous factory had not yet resolved.

The repair gives every load a generation-owned operation containing its task-creation promise, loading task, document promise, document, page, and render task. Replacement, retry, fallback, paper change, and unmount detach and invalidate that operation before awaiting its idempotent disposal. The loading-task promise receives a rejection observer as soon as task creation resolves. Destroy waits for pending task creation and disposes the eventual obsolete task. A permanently destroyed controller cannot be restarted. React effect generations gate state, diagnostics, and canvas access, and both load and cleanup promises have explicit completion/error observers. Thus discarded A can terminate only A resources, while active B remains authoritative.

When both a non-production build and `NEXT_PUBLIC_BLUEPRINT_READER_DIAGNOSTICS=1` are present, a small diagnostics disclosure shows document-load count/duration, page-render count, render-cancellation count, first-page render duration, and safely inferred request/Range mode from Resource Timing. It is disabled by default, uploads nothing, persists nothing, and contains no paper title, DOI, author, note content, URL, path, or PDF content. Real-browser request counts and Range use still require manual inspection because Resource Timing status visibility varies by browser.

## Automated validation

This release note preserves historical execution context; it is not the current status authority. The generated [Current Release Status](../CURRENT_RELEASE_STATUS.md) is derived from the canonical machine-readable manifest and separates automated, manual, source-control, publication, and operational state.

Two successful historical smoke records conflict and are intentionally retained: the 2026-07-25 lifecycle-repair record reports 97 passed, 1 optional warning, and 0 failed, while the earlier foundation record reports 98 passed, 0 warnings, and 0 failed. Both establish successful execution, but neither pass/warning count silently replaces the other.

Other lifecycle-repair records report 226 tracked entries inspected; 13 focused PDF API tests passed; 24 focused release/version tests passed; 526 full Python tests passed with no failures or skips; frontend lint and production build passed; and 30 Node tests passed. These values remain historical evidence here and structured evidence in the canonical manifest.

The repair dependency update audited 496 packages and reported 15 findings: 2 low and 13 high. No audit fix, forced resolution, copied worker, or unrelated dependency upgrade was performed.

Initial failures and reruns were retained rather than hidden:

- The first repair baseline commands resolved `python` to an inaccessible Windows launcher. Rerunning through the repository virtual environment reached the tests.
- The first repair hygiene and smoke reruns could not find Git from Python subprocesses, and pytest could not use the sandboxed user temp directory. Adding the installed Git directory to the process `PATH` and using a disposable workspace temp root corrected the environment without a code change.
- The first repair `npm ci` attempt timed out under restricted network access. The approved network-enabled rerun installed 495 packages and audited 496.
- Python API/full-suite runs passed but the local optional `markitdown`/`onnxruntime` import emitted a post-test native access-violation trace; pytest still returned exit code 0. This environment warning is unrelated to the Reader repair.
- The first focused controller invocation used a stale portable-Node directory and did not start; rerunning with the configured runtime passed all 9 controller tests.
- A PowerShell `npm` alias selected an unsigned `npm.ps1` and was blocked before dependency installation; invoking the supported `npm.cmd` directly succeeded.
- The first worker-asset assertion produced an invalid doubled Windows drive path; `fileURLToPath` corrected the test and the identical focused file passed 5/5.
- The first lint run found two direct effect-state updates and one explicit `any`; the implementation was corrected and the identical lint command passed.
- One initially failing smoke attempt still required the removed probe. Later successful smoke records disagree on pass/warning totals, so the canonical manifest preserves the conflict explicitly.

## Manual validation evidence

PR #4 records successful real-PDF first-page rendering, previous/next navigation, direct page entry, zoom, repeated Reader entry/exit, different-paper transition, rapid page navigation, native fallback exclusivity, and absence of the prior worker-termination error. API offline/restart recovery, large-PDF behavior, detailed Range inspection, and the separate Streamlit regression have no completed current record. The canonical aggregate is therefore PARTIALLY VERIFIED, with passed and pending items represented individually.

Clean-PC restore is a recurring operational procedure, not inferred from setup or readiness scripts. Current tag and GitHub Release publication state is reported only in the [Current Release Status](../CURRENT_RELEASE_STATUS.md).

## Known limitations and explicit non-goals

The Reader renders one page at a time and does not add text selection/search, text or annotation layers, highlights, comment pins, note-to-page links, thumbnails, continuous scrolling, persistent position, OCR, LLM features, mobile redesign, or write behavior. Full screen-reader PDF accessibility is outside this foundation; the canvas supplies only a page description. Resource Timing may not expose response status in every browser, so diagnostics can report `status-unavailable`.

Storage formats, paper identity, managed-root security, API routes and response shapes, CSV/note/project/tag/snapshot/cache formats, local-first single-user architecture, and every Streamlit write workflow remain unchanged. No user PDF, real note, or private library index is used by the automated tests.
